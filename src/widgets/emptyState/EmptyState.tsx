import styles from './EmptyState.module.css'

const EmptyState = () => {
	return <div className={styles.empty}>📭 Задач пока нет — добавьте первую</div>
}

export default EmptyState
